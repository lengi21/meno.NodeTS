import { Injectable } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service.js';
@Injectable() export class AuditService { constructor(private readonly prisma: PrismaService) {} list(restaurantId:string, search=''){ return this.prisma.auditEvent.findMany({where:{restaurantId,...(search?{OR:[{action:{contains:search,mode:'insensitive'}},{entityType:{contains:search,mode:'insensitive'}}]}:{})},include:{member:{select:{firstName:true,lastName:true}}},orderBy:{createdAt:'desc'},take:100}).then(rows=>rows.map(row=>({...row,memberName:row.member?`${row.member.firstName} ${row.member.lastName}`:'System'}))); } }
